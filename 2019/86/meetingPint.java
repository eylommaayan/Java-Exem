public class meetingPint {

    public static void main(String[] args) {
        
        
    }

    public static int meetingPint(int[] a, int[] b) {
        int min = Integer.MAX_VALUE;
        int start = 0;
        int end = Math.min(a.length, b.length)-1;
   
        while (start <= end) {
            int mid = (start+end)/2;
            if (a[mid] == b[mid])

        }
        
    }
}